apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: fieldserviceit
type: Opaque
stringData:
  DATABASE_URL: mysql://app:CHANGEME@mysql:3306/fieldserviceit
  JWT_SECRET: CHANGE-ME-IN-PRODUCTION
---
# This file should be encrypted with SOPS or stored in Vault.
# Do NOT commit plaintext secrets to version control.
